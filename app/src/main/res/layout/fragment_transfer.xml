<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/recipientAccountEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Recipient Account Number"
        android:inputType="number"
        android:layout_marginBottom="16dp" />

    <EditText
        android:id="@+id/transferAmountEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Transfer Amount (₹)"
        android:inputType="number"
        android:layout_below="@id/recipientAccountEditText"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/transferButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Transfer Money"
        android:layout_below="@id/transferAmountEditText" />

</RelativeLayout>
